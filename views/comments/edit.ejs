<% include ../partials/header %>

<div class="ui container">
    <form class="ui form" action="/<%= category %>/<%= post_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
        <div class="field">
            <p>Комментарий:</p>
        </div>
        <div class="field">
            <textarea name="comment[description]" required><%= comment.description %></textarea>
        </div>
        <div class="field">
            <button class="ui basic violet button">Изменить</button>
        </div>
    </form>
</div>

<% include ../partials/footer %>